server:
  port: 8083
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/apricity
  application:
    name: post-svc
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/haze-org